#include <iostream>
#include "CarData.h"


int main() {
    for (car : cars){
        int runningtotal = 0;
        runningtotal += car.mpgrnk * mpgpref;
        if car.sizeclass == "suv"{

        }
        if runningtotal > bestcarranking
    }
    return 0;
}
